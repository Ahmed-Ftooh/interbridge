import 'package:flutter/widgets.dart';

/// Legacy interpreter registration step retained only for git history.
/// The new multi-step experience lives in interpreter_registration_flow.dart.
class ShiftsStep extends StatelessWidget {
  const ShiftsStep({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}

import 'package:flutter/material.dart';

class AuthWebPalette {
  // 1. Brand Colors
  static const Color primary = Color(0xFF0F4C81); // InterBridge Deep Blue
  static const Color primaryDark = Color(0xFF0A365C); // For sidebar gradients
  static const Color primaryLight = Color(0xFFE0E7FF); // Soft active tints
  
  static const Color success = Color(0xFF10B981); // Green for completed steps

  // 2. Backgrounds
  static const Color bgBase = Color(0xFFF8FAFC); // Main light slate background
  static const Color surface = Color(0xFFFFFFFF); // White cards
  
  // 3. Borders & Dividers
  static const Color border = Color(0xFFE2E8F0); // Standard thin borders
  static const Color borderDark = Color(0xFFCBD5E1); // Hover borders
  
  // 4. Typography
  static const Color textPrimary = Color(0xFF0F172A); // Main headings
  static const Color textSecondary = Color(0xFF475569); // Subtitles / body
  static const Color textMuted = Color(0xFF94A3B8); // Inactive step text
}